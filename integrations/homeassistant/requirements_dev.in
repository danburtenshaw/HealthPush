# HealthPush Home Assistant integration — dev tooling (source file)
# Keep in sync with pyproject.toml configuration.
#
# This is the human-edited source. The hash-pinned lockfile
# (requirements_dev.txt) is what CI installs. Regenerate after edits:
#   uv pip compile requirements_dev.in --generate-hashes --universal \
#     --python-version 3.14 --output-file requirements_dev.txt
#
# Renovate bumps pinned versions in the .txt lockfile automatically.

ruff==0.15.9
mypy==1.20.0
bandit[toml]==1.9.4
pytest-cov==7.1.0
pytest-asyncio==1.3.0
# homeassistant is installed for its type stubs — mypy strict on the custom
# component needs to see ConfigFlow, HomeAssistant, and the helpers modules
# as real types, not Any. aiohttp comes in transitively.
# pytest-homeassistant-custom-component is intentionally *not* pinned: its
# transitive acme/josepy pins conflict at import time. Add it back alongside
# a matching HA version when tests start using its fixtures.
homeassistant>=2026.1.3
